apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    name: observability
    # Istio: enable sidecar injection so apps, collector, postgres get envoy proxy and mTLS
    istio-injection: enabled
